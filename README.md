# Project Dream (MVP v7)

Updates:
- Navigation: **Home** (was Project Dream).
- Progress: only counts when you click **Finish** on each page (Finish is a toggle).
- Home “10 steps” is clickable → jumps to each page.
- Goals is ONE step: Goals (1/3/5).
- Dream house and FIRE are separate tabs.
- Added Health & fitness tab.
- Dream house redesigned with structured fields + checklists.

Run:
```bash
npm install
npm run dev
```
